# Run to create: agenix-rekey edit ./wallabag-secrets.age
# Contents (KEY=value format):
#   POSTGRES_PASSWORD=<postgres password (used by wallabag-db)>
#   SYMFONY__ENV__DATABASE_PASSWORD=<postgres password (same value, used by wallabag app)>
