# Create the DuckDNS token secret here, from inside the devenv shell:
#   agenix edit ./duckdns-token.age     # paste ONLY the token (from duckdns.org), save
#   agenix rekey                         # re-encrypt to host keys (needs the yubikey)
