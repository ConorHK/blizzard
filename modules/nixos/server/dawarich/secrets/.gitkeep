# Run to create: agenix-rekey edit ./dawarich-secrets.age
# Contents (KEY=value format):
#   POSTGRES_PASSWORD=<postgres password>
#   DATABASE_PASSWORD=<postgres password (same value, used by app/sidekiq containers)>
